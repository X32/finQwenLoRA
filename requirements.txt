# ============================================================================
# FinQwen LoRA训练项目依赖包
# ============================================================================
# Python版本要求: 3.7-3.11 (推荐3.8-3.10)
# GPU要求: NVIDIA GPU, CUDA 11.8+
# ============================================================================

# ------------------- 核心深度学习框架 -------------------
# PyTorch核心库 (根据CUDA版本选择)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ------------------- Transformers和PEFT库 -------------------
# HuggingFace Transformers (模型和分词器)
transformers>=4.35.0
# Tokenizers库
tokenizers>=0.15.0
# datasets库 (可选，用于高级数据处理)
datasets>=2.14.0

# ------------------- PEFT和量化库 -------------------
# PEFT库 (LoRA, QLoRA, AdaLoRA支持)
peft>=0.7.0
# 加速库 (分布式训练和混合精度)
accelerate>=0.25.0
# BitsAndBytes (4-bit量化，QLoRA必需)
bitsandbytes>=0.41.0

# ------------------- 阿里云ModelScope (国内访问更快) -------------------
# ModelScope库 (阿里云模型下载)
modelscope>=1.0.0

# ------------------- 数据处理库 -------------------
# JSON和数据处理
jsonlines>=4.0.0
# pandas (数据分析)
pandas>=2.0.0
# numpy (数值计算)
numpy>=1.24.0
# scipy (科学计算)
scipy>=1.10.0

# ------------------- 实用工具库 -------------------
# 进度条显示
tqdm>=4.66.0
# 日志和配置
pyyaml>=6.0
# wandb (训练监控，可选)
wandb>=0.15.0
# tensorboard (训练可视化，可选)
tensorboard>=2.14.0

# ------------------- DeepSpeed (可选，分布式训练) -------------------
# DeepSpeed (大规模分布式训练，可选安装)
# deepspeed>=0.12.0

# ------------------- 文本处理库 -------------------
# jieba (中文分词)
jieba>=0.42.0
# sentencepiece (分词器)
sentencepiece>=0.1.99
# sacremoses (文本标准化)
sacremoses>=0.0.53

# ------------------- 网络和下载工具 -------------------
# requests (HTTP请求)
requests>=2.31.0
# huggingface-hub (模型下载)
huggingface-hub>=0.17.0

# ------------------- 开发工具 (可选) -------------------
# pytest (测试)
# pytest>=7.4.0
# black (代码格式化)
# black>=23.0.0
# isort (import排序)
# isort>=5.12.0
# flake8 (代码检查)
# flake8>=6.1.0