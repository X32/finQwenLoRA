# 📚 模块5：实战应用与扩展

## 🎯 学习目标
掌握LoRA微调的实战应用技巧、性能调优方法、故障排除策略，达到能独立完成项目部署和功能扩展的工程深度。

---

## 🚀 实战训练完整流程

### 1.1 环境准备和配置

**硬件需求评估**：

```bash
# 显存需求计算表
┌─────────────┬──────────┬─────────────┬──────────┐
│ 模型大小     │ 训练类型  │ 最低显存     │ 推荐显存   │
├─────────────┼──────────┼─────────────┼──────────┤
│ Qwen-0.5B   │ LoRA     │ 6GB         │ 8GB      │
│ Qwen-1.5B   │ LoRA     │ 8GB         │ 11GB      │
│ Qwen-7B     │ LoRA     │ 13GB        │ 16GB      │
│ Qwen-7B     │ QLoRA    │ 8GB         │ 11GB      │
│ Qwen-14B    │ QLoRA    │ 12GB        │ 16GB      │
└─────────────┴──────────┴─────────────┴──────────┘
```

**软件环境配置**：

```bash
# 1. 创建虚拟环境
python -m venv lora_training
source lora_training/bin/activate  # Linux/Mac
# 或 lora_training\Scripts\activate  # Windows

# 2. 安装核心依赖
pip install torch>=2.0.0
pip install transformers>=4.35.0
pip install peft>=0.6.0
pip install accelerate>=0.24.0
pip install bitsandbytes>=0.41.0

# 3. 验证安装
python -c "import torch; print(f'PyTorch: {torch.__version__}')"
python -c "import transformers; print(f'Transformers: {transformers.__version__}')"
```

### 1.2 数据准备实战

**数据格式转换**：

```python
# 原始数据转换为训练格式
import json

def convert_to_training_format(raw_data, output_path):
    """
    将原始对话数据转换为训练格式
    
    输入示例：
    [
        {"question": "如何查询基金信息？", "answer": "可以使用SELECT * FROM funds..."},
        {"question": "什么是基金的夏普比率？", "answer": "夏普比率是..."}
    ]
    
    输出格式：
    [
        {
            "conversations": [
                {"from": "human", "value": "如何查询基金信息？"},
                {"from": "gpt", "value": "可以使用SELECT * FROM funds..."}
            ]
        }
    ]
    """
    training_data = []
    
    for item in raw_data:
        conversation = {
            "conversations": [
                {"from": "human", "value": item["question"]},
                {"from": "gpt", "value": item["answer"]}
            ]
        }
        training_data.append(conversation)
    
    # 保存为JSON格式
    with open(output_path, 'w', encoding='utf-8') as f:
        json.dump(training_data, f, ensure_ascii=False, indent=2)
    
    print(f"转换完成：{len(training_data)}条数据")
    return training_data

# 使用示例
raw_sql_data = [
    {"question": "查询所有基金的基本信息", "answer": "SELECT * FROM fund_basic_info;"},
    {"question": "获取规模大于10亿的基金", "answer": "SELECT * FROM fund_basic_info WHERE fund_scale > 1000000000;"}
]

convert_to_training_format(raw_sql_data, 'sql_lora_train.json')
```

**数据质量检查**：

```python
def check_data_quality(data_path):
    """数据质量检查"""
    with open(data_path, 'r', encoding='utf-8') as f:
        data = json.load(f)
    
    issues = []
    
    for i, item in enumerate(data):
        # 检查1：必需字段
        if 'conversations' not in item:
            issues.append(f"样本{i}:缺少conversations字段")
        
        # 检查2：对话结构
        if len(item['conversations']) < 2:
            issues.append(f"样本{i}:对话少于2轮")
        
        # 检查3：角色平衡
        roles = [msg['from'] for msg in item['conversations']]
        if roles.count('human') != roles.count('gpt'):
            issues.append(f"样本{i}:用户和助手数量不匹配")
        
        # 检查4：内容长度
        for msg in item['conversations']:
            if len(msg['value']) < 5:
                issues.append(f"样本{i}:内容过短")
            if len(msg['value']) > 2000:
                issues.append(f"样本{i}:内容过长")
    
    if issues:
        print("发现数据质量问题：")
        for issue in issues[:10]:  # 只显示前10个问题
            print(f"  - {issue}")
        print(f"总共{len(issues)}个问题")
    else:
        print("数据质量检查通过！")
    
    return len(issues) == 0

# 使用示例
check_data_quality('sql_lora_train.json')
```

### 1.3 训练启动和监控

**训练脚本使用**：

```bash
# 基础训练（11GB显存配置）
cd solutions/7_不会ML/app/train_lora

# 修改run_lora_1080Ti.sh中的数据路径
DATA='../data/lora_data/your_data.json'

# 启动训练
bash run_lora_1080Ti.sh

# 训练输出示例：
# === GTX 1080 Ti 训练配置 ===
# GPU: NVIDIA GTX 1080 Ti (11GB显存)
# 模型: Qwen-1.5B-Chat
# 开始训练...
# {'train_runtime': 2345.6, 'train_samples_per_second': 1.23, 'train_loss': 0.456}
```

**训练监控脚本**：

```python
import time
import subprocess
import json

class TrainingMonitor:
    """训练过程监控"""
    
    def __init__(self, log_file='training.log'):
        self.log_file = log_file
        self.start_time = time.time()
        
    def monitor_training(self, process):
        """监控训练进度"""
        print("训练监控启动...")
        print("=" * 50)
        
        while True:
            # 检查进程状态
            if process.poll() is not None:
                print("\n训练完成！")
                self.print_summary()
                break
            
            # 读取训练日志
            try:
                with open(self.log_file, 'r') as f:
                    logs = f.readlines()
                    if logs:
                        latest_log = logs[-1]
                        if 'loss' in latest_log:
                            print(f"\r{latest_log.strip()}", end='')
            except:
                pass
            
            time.sleep(5)  # 每5秒更新一次
    
    def print_summary(self):
        """打印训练摘要"""
        elapsed_time = time.time() - self.start_time
        hours, remainder = divmod(elapsed_time, 3600)
        minutes, seconds = divmod(remainder, 60)
        
        print(f"\n训练时间: {int(hours)}小时{int(minutes)}分钟{int(seconds)}秒")
        
        # 分析训练日志
        losses = []
        try:
            with open(self.log_file, 'r') as f:
                for line in f:
                    if 'loss' in line:
                        loss_value = float(line.split('loss')[-1].strip())
                        losses.append(loss_value)
            
            if losses:
                print(f"初始Loss: {losses[0]:.4f}")
                print(f"最终Loss: {losses[-1]:.4f}")
                print(f"Loss下降: {losses[0] - losses[-1]:.4f} ({(1 - losses[-1]/losses[0])*100:.1f}%)")
        except:
            print("无法提取训练指标")

# 使用示例
monitor = TrainingMonitor()
# process = subprocess.Popen(['bash', 'run_lora_1080Ti.sh'], stdout=subprocess.PIPE)
# monitor.monitor_training(process)
```

---

## 🔧 性能调优技巧

### 2.1 显存优化策略

**显存不足的解决方案**：

```python
# 方案1：减少序列长度
# 原始配置
MAX_LENGTH=512  # 需要8GB显存

# 优化配置
MAX_LENGTH=256  # 只需要4GB显存

# 方案2：增加梯度累积
# 原始配置
BATCH_SIZE=1
GRADIENT_ACCUMULATION_STEPS=4

# 优化配置  
BATCH_SIZE=1
GRADIENT_ACCUMULATION_STEPS=16  # 有效batch_size = 1×16 = 16

# 方案3：启用DeepSpeed CPU卸载
accelerate launch \
  --offload_optimizer_device 'cpu' \
  --offload_param_device 'cpu' \
  finetune_qwen.py ...

# 方案4：使用量化模型
# LoRA → QLoRA
# 显存节省：40-50%
```

**显存监控脚本**：

```python
import torch
import psutil
import time

def monitor_memory_usage(interval=5):
    """实时监控显存和内存使用"""
    while True:
        # 显存使用
        if torch.cuda.is_available():
            gpu_memory = torch.cuda.memory_allocated() / 1024**3  # GB
            gpu_memory_reserved = torch.cuda.memory_reserved() / 1024**3
            gpu_memory_total = torch.cuda.get_device_properties(0).total_memory / 1024**3
            
            print(f"GPU显存: {gpu_memory:.2f}GB / {gpu_memory_total:.2f}GB "
                  f"(预留: {gpu_memory_reserved:.2f}GB)")
        
        # 系统内存
        cpu_memory = psutil.virtual_memory().used / 1024**3  # GB
        cpu_memory_total = psutil.virtual_memory().total / 1024**3
        
        print(f"CPU内存: {cpu_memory:.2f}GB / {cpu_memory_total:.2f}GB")
        print("-" * 40)
        
        time.sleep(interval)

# 使用示例
# monitor_memory_usage()
```

### 2.2 训练速度优化

**多进程数据加载**：

```python
from torch.utils.data import DataLoader

# 优化前：单进程加载
train_loader = DataLoader(
    train_dataset,
    batch_size=1,
    shuffle=True
)

# 优化后：多进程加载
train_loader = DataLoader(
    train_dataset,
    batch_size=1,
    shuffle=True,
    num_workers=4,        # 4个进程并行加载
    pin_memory=True,       # 锁页内存，加速CPU→GPU传输
    persistent_workers=True # 保持worker进程，减少重启开销
)

# 加速效果：实测提升30-50%
```

**混合精度训练优化**：

```python
from torch.cuda.amp import autocast, GradScaler

# 启用混合精度训练
scaler = GradScaler()

for batch in train_loader:
    # 前向传播：自动混合精度
    with autocast():
        outputs = model(batch)
        loss = calculate_loss(outputs, batch)
    
    # 反向传播：梯度裁剪
    scaler.scale(loss).backward()
    
    # 参数更新：反量化梯度
    scaler.step(optimizer)
    scaler.update()
    optimizer.zero_grad()

# 加速效果：训练速度提升2-3倍，显存节省40%
```

### 2.3 模型效果优化

**超参数调优策略**：

```python
# 网格搜索最佳参数
best_params = None
best_loss = float('inf')

# 参数搜索空间
learning_rates = [1e-5, 5e-5, 1e-4]
lora_ranks = [8, 16, 32]
lora_alphas = [8, 16, 32]

for lr in learning_rates:
    for rank in lora_ranks:
        for alpha in lora_alphas:
            print(f"测试参数: lr={lr}, rank={rank}, alpha={alpha}")
            
            # 训练模型
            model = train_model(
                learning_rate=lr,
                lora_r=rank,
                lora_alpha=alpha
            )
            
            # 评估性能
            val_loss = evaluate_model(model, val_dataset)
            
            if val_loss < best_loss:
                best_loss = val_loss
                best_params = {'lr': lr, 'rank': rank, 'alpha': alpha}
                print(f"发现更好参数！loss: {val_loss:.4f}")

print(f"最佳参数: {best_params}")
```

**数据增强策略**：

```python
def augment_training_data(original_data):
    """数据增强提升模型泛化能力"""
    augmented_data = []
    
    for item in original_data:
        original_conversation = item['conversations']
        
        # 策略1：同义改写
        for conversation in original_conversation:
            if conversation['from'] == 'human':
                # 简单的同义词替换
                augmented = rewrite_with_synonyms(conversation['value'])
                if augmented != conversation['value']:
                    new_conversation = original_conversation.copy()
                    new_conversation[original_conversation.index(conversation)] = {
                        'from': 'human',
                        'value': augmented
                    }
                    augmented_data.append({'conversations': new_conversation})
        
        # 策略2：回译增强
        # (需要翻译API支持)
        # back_translated = back_translate(conversation['value'])
        
        # 策略3：模板变体
        templates = [
            "请告诉我：{}",
            "如何理解：{}",
            "解释一下：{}"
        ]
        
        for template in templates:
            for conversation in original_conversation:
                if conversation['from'] == 'human':
                    new_value = template.format(conversation['value'])
                    new_conversation = original_conversation.copy()
                    new_conversation[original_conversation.index(conversation)] = {
                        'from': 'human',
                        'value': new_value
                    }
                    augmented_data.append({'conversations': new_conversation})
    
    return original_data + augmented_data

# 使用示例
# original_data = json.load(open('sql_lora_train.json'))
# augmented_data = augment_training_data(original_data)
# json.dump(augmented_data, open('augmented_data.json', 'w'))
```

---

## 🛠️ 故障排除方法

### 3.1 常见错误及解决方案

**错误1：CUDA显存不足**

```python
# 错误信息
RuntimeError: CUDA out of memory. Tried to allocate 512.00 MiB

# 解决方案层级
# 方案1：立即生效 - 减少batch size
BATCH_SIZE=1  # 从2减到1

# 方案2：中度优化 - 启用梯度检查点
accelerate launch \
  --gradient_checkpointing \
  finetune_qwen.py ...

# 方案3：深度优化 - 使用量化模型
python finetune_qwen.py \
  --quantization_bit 4 \
  --use_lora \
  ...

# 方案4：终极方案 - 使用云端GPU
# 推荐平台：AutoDL、恒源云、GasGPU
```

**错误2：训练不收敛**

```python
# 症状：Loss不下降或震荡
# Epoch 1: loss=2.345
# Epoch 2: loss=2.678 (上升！)
# Epoch 3: loss=2.123 (下降)
# Epoch 4: loss=2.456 (又上升！)

# 诊断和解决
def diagnose_training_issues():
    issues = []
    solutions = []
    
    # 检查1：学习率是否过大
    if learning_rate > 1e-3:
        issues.append("学习率过大")
        solutions.append("降低学习率到1e-5到1e-4之间")
    
    # 检查2：数据质量
    if check_data_quality(data_path) == False:
        issues.append("数据质量有问题")
        solutions.append("检查数据格式和内容质量")
    
    # 检查3：梯度爆炸
    if any(grad.norm() > 100 for grad in model.parameters()):
        issues.append("梯度爆炸")
        solutions.append("启用梯度裁剪：max_grad_norm=1.0")
    
    # 检查4：batch size过小
    if batch_size == 1 and gradient_accumulation_steps < 4:
        issues.append("有效batch size过小")
        solutions.append("增加gradient_accumulation_steps到8或16")
    
    return issues, solutions

# 使用示例
# issues, solutions = diagnose_training_issues()
# for issue, solution in zip(issues, solutions):
#     print(f"问题: {issue}")
#     print(f"解决: {solution}")
```

**错误3：模型推理结果异常**

```python
# 症状：模型输出重复或无意义
# 输入: "查询所有基金"
# 输出: "查询所有基金查询所有基金查询所有基金..."

# 诊断脚本
def diagnose_model_inference(model, tokenizer, test_questions):
    """诊断模型推理问题"""
    print("模型推理诊断...")
    
    for question in test_questions:
        print(f"\n测试问题: {question}")
        
        # 生成回复
        inputs = tokenizer(question, return_tensors="pt")
        outputs = model.generate(**inputs, max_new_tokens=100)
        response = tokenizer.decode(outputs[0], skip_special_tokens=True)
        
        print(f"模型回复: {response}")
        
        # 检查问题
        if len(set(response.split())) < len(response.split()) / 2:
            print("⚠️ 警告：回复中重复内容过多")
        
        if len(response) < 10:
            print("⚠️ 警告：回复过短")
        
        if "用户输入" in response or "human" in response:
            print("⚠️ 警告：可能泄露训练模板")

# 使用示例
# test_questions = ["查询基金信息", "获取基金规模"]
# diagnose_model_inference(model, tokenizer, test_questions)
```

### 3.2 调试技巧

**训练过程可视化**：

```python
import matplotlib.pyplot as plt
import json

def visualize_training_process(log_file='training_logs.jsonl'):
    """可视化训练过程"""
    steps = []
    losses = []
    learning_rates = []
    
    # 读取训练日志
    with open(log_file, 'r') as f:
        for line in f:
            log = json.loads(line)
            if 'loss' in log:
                steps.append(log.get('step', 0))
                losses.append(log['loss'])
                learning_rates.append(log.get('learning_rate', 0))
    
    # 创建图表
    fig, axes = plt.subplots(2, 1, figsize=(12, 10))
    
    # Loss曲线
    axes[0].plot(steps, losses, label='Training Loss')
    axes[0].set_xlabel('Steps')
    axes[0].set_ylabel('Loss')
    axes[0].set_title('Training Loss Over Time')
    axes[0].legend()
    axes[0].grid(True)
    
    # 学习率曲线
    axes[1].plot(steps, learning_rates, label='Learning Rate', color='orange')
    axes[1].set_xlabel('Steps')
    axes[1].set_ylabel('Learning Rate')
    axes[1].set_title('Learning Rate Schedule')
    axes[1].legend()
    axes[1].grid(True)
    
    plt.tight_layout()
    plt.savefig('training_visualization.png')
    print("训练可视化保存到 training_visualization.png")

# 使用示例
# visualize_training_process()
```

**断点续训技巧**：

```bash
# 训练中断后恢复
# 1. 找到最后保存的检查点
ls -lt output/sql_lora_qwen2.5/checkpoint-*/

# 2. 从检查点恢复训练
accelerate launch \
  finetune_qwen.py \
  --model_name_or_path ./models/Qwen2.5-1.5B-Instruct \
  --data_path ../data/lora_data/sql-lora-train-end.json \
  --output_dir output/sql_lora_qwen2.5 \
  --resume_from_checkpoint output/sql_lora_qwen2.5/checkpoint-800 \
  ...

# 3. 验证恢复成功
# 观察训练日志中的step是否从检查点继续
```

---

## 🌐 模型部署方案

### 4.1 本地部署

**模型合并和保存**：

```python
from peft import PeftModel
from transformers import AutoModelForCausalLM, AutoTokenizer

def merge_and_save_model(base_model_path, lora_adapter_path, output_path):
    """合并LoRA适配器并保存完整模型"""
    
    # 1. 加载基础模型
    print("加载基础模型...")
    base_model = AutoModelForCausalLM.from_pretrained(
        base_model_path,
        torch_dtype=torch.float16,
        device_map="auto"
    )
    
    # 2. 加载LoRA适配器
    print("加载LoRA适配器...")
    model = PeftModel.from_pretrained(base_model, lora_adapter_path)
    
    # 3. 合并模型
    print("合并模型...")
    merged_model = model.merge_and_unload()
    
    # 4. 保存完整模型
    print("保存模型...")
    merged_model.save_pretrained(output_path)
    
    # 5. 保存tokenizer
    tokenizer = AutoTokenizer.from_pretrained(base_model_path)
    tokenizer.save_pretrained(output_path)
    
    print(f"模型已保存到: {output_path}")

# 使用示例
# merge_and_save_model(
#     base_model_path="./models/Qwen2.5-1.5B-Instruct",
#     lora_adapter_path="output/sql_lora_qwen2.5/checkpoint-1000",
#     output_path="./models/merged_sql_qwen"
# )
```

**推理服务部署**：

```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel
import uvicorn

app = FastAPI(title="SQL Qwen API")

class GenerationRequest(BaseModel):
    question: str
    max_length: int = 512
    temperature: float = 0.7

class GenerationResponse(BaseModel):
    answer: str
    model: str

# 加载模型
model, tokenizer = load_model()

@app.post("/generate", response_model=GenerationResponse)
async def generate_sql(request: GenerationRequest):
    """生成SQL查询"""
    try:
        # 格式化输入
        messages = [
            {"role": "system", "content": "你是一个SQL生成专家"},
            {"role": "user", "content": request.question}
        ]
        
        # 生成回复
        inputs = tokenizer.apply_chat_template(
            messages,
            return_tensors="pt"
        )
        
        outputs = model.generate(
            **inputs,
            max_new_tokens=request.max_length,
            temperature=request.temperature,
            do_sample=True
        )
        
        # 解码结果
        answer = tokenizer.decode(outputs[0], skip_special_tokens=True)
        
        return GenerationResponse(
            answer=answer,
            model="SQL-Qwen-1.5B-LoRA"
        )
        
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))

# 启动服务
if __name__ == "__main__":
    uvicorn.run(app, host="0.0.0.0", port=8000)
```

### 4.2 云端部署

**Docker容器化部署**：

```dockerfile
# Dockerfile
FROM python:3.10-slim

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制模型和代码
COPY models/ ./models/
COPY app/ ./app/

# 暴露端口
EXPOSE 8000

# 启动服务
CMD ["python", "app/inference_server.py"]
```

```bash
# 构建和运行
docker build -t sql-qwen-api .
docker run -p 8000:8000 --gpus all sql-qwen-api
```

**API网关部署**：

```python
# 使用云服务商的API网关
# 示例：阿里云函数计算

import json
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer

# 全局加载模型（复用实例）
model = None
tokenizer = None

def initialize():
    """初始化模型"""
    global model, tokenizer
    model_path = "/opt/python/models/qwen-sql-lora"
    model = AutoModelForCausalLM.from_pretrained(model_path)
    tokenizer = AutoTokenizer.from_pretrained(model_path)

def handler(event, context):
    """API处理器"""
    global model, tokenizer
    
    # 解析请求
    body = json.loads(event["body"])
    question = body.get("question", "")
    
    # 生成回复
    messages = [
        {"role": "system", "content": "你是一个SQL生成专家"},
        {"role": "user", "content": question}
    ]
    
    inputs = tokenizer.apply_chat_template(messages, return_tensors="pt")
    outputs = model.generate(**inputs, max_new_tokens=512)
    answer = tokenizer.decode(outputs[0], skip_special_tokens=True)
    
    return {
        "statusCode": 200,
        "body": json.dumps({"answer": answer})
    }
```

---

## 🚀 功能扩展思路

### 5.1 多任务学习扩展

**支持多个微调任务**：

```python
class MultiTaskLoRA(nn.Module):
    """多任务LoRA架构"""
    
    def __init__(self, base_model, task_configs):
        super().__init__()
        self.base_model = base_model
        self.tasks = {}
        
        # 为每个任务创建独立的LoRA适配器
        for task_name, config in task_configs.items():
            lora_adapter = create_lora_adapter(
                base_model,
                rank=config['rank'],
                alpha=config['alpha'],
                target_modules=config['modules']
            )
            self.tasks[task_name] = lora_adapter
    
    def forward(self, x, task_name):
        """指定任务的前向传播"""
        lora_adapter = self.tasks[task_name]
        return lora_adapter(x)

# 使用示例
task_configs = {
    'sql_generation': {
        'rank': 16,
        'alpha': 8,
        'modules': ['q_proj', 'v_proj', 'o_proj']
    },
    'ner_extraction': {
        'rank': 32,
        'alpha': 16,
        'modules': ['q_proj', 'k_proj', 'v_proj']
    }
}

multi_task_model = MultiTaskLoRA(base_model, task_configs)
```

### 5.2 知识蒸馏集成

**使用大模型指导小模型训练**：

```python
def distill_knowledge(teacher_model, student_model, data_loader):
    """知识蒸馏训练"""
    
    temperature = 3.0  # 温度参数
    alpha = 0.7       # 蒸馏损失权重
    
    for batch in data_loader:
        # 教师模型预测
        with torch.no_grad():
            teacher_logits = teacher_model(batch)
            teacher_probs = torch.softmax(teacher_logits / temperature, dim=-1)
        
        # 学生模型预测
        student_logits = student_model(batch)
        student_probs = torch.softmax(student_logits / temperature, dim=-1)
        
        # 蒸馏损失
        distill_loss = kl_div(student_probs, teacher_probs) * (temperature ** 2)
        
        # 学生损失
        student_loss = cross_entropy(student_logits, batch['labels'])
        
        # 组合损失
        total_loss = alpha * distill_loss + (1 - alpha) * student_loss
        
        # 反向传播和更新
        total_loss.backward()
        optimizer.step()
        optimizer.zero_grad()

# 使用场景：用7B模型指导1.5B模型训练
```

### 5.3 持续学习机制

**支持增量学习和在线更新**：

```python
class ContinualLearner:
    """持续学习器"""
    
    def __init__(self, base_model, memory_size=1000):
        self.model = base_model
        self.memory = []  # 经验回放缓冲区
        self.memory_size = memory_size
        
    def add_to_memory(self, samples):
        """添加新样本到记忆"""
        self.memory.extend(samples)
        if len(self.memory) > self.memory_size:
            # 随机淘汰旧样本
            self.memory = random.sample(self.memory, self.memory_size)
    
    def continual_train(self, new_data, epochs=1):
        """持续训练"""
        # 从记忆中采样旧数据
        memory_samples = random.sample(self.memory, min(len(self.memory), len(new_data)))
        
        # 混合新旧数据
        mixed_data = new_data + memory_samples
        
        # 训练
        for epoch in range(epochs):
            for batch in create_batches(mixed_data):
                loss = self.model(batch)
                loss.backward()
                self.optimizer.step()
                self.optimizer.zero_grad()
        
        # 更新记忆
        self.add_to_memory(new_data)

# 应用场景：模型上线后持续从用户反馈中学习
```

---

## 💡 模块5总结

### 实战技能掌握

1. **环境配置**：硬件需求评估、软件环境搭建、依赖管理
2. **数据处理**：格式转换、质量检查、数据增强
3. **训练执行**：启动训练、监控进度、性能调优
4. **故障排除**：常见错误诊断、调试技巧、断点续训
5. **模型部署**：本地部署、云端部署、API服务
6. **功能扩展**：多任务学习、知识蒸馏、持续学习

### 工程能力要求

1. **独立部署**：能独立完成从训练到部署的完整流程
2. **性能调优**：能根据实际情况优化训练和推理性能
3. **问题诊断**：能快速定位和解决训练部署中的问题
4. **架构设计**：能设计复杂的模型架构和训练流程
5. **创新应用**：能将技术应用到新的业务场景

---

**准备好进入模块6：面试重点与技巧了吗？** 接下来我们将整理面试必备的知识点和技巧！